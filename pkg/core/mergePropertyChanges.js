/**
 * TODO -- will be used to do a merge of incoming props
 * @param existing
 * @param incoming
 */
export function mergePropertyChanges(existing, incoming) {
    return Object.assign({}, existing, incoming);
}
//# sourceMappingURL=mergePropertyChanges.js.map